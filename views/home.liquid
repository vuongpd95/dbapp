<!DOCTYPE html>
<html>
<head>
    <title>Deployment Table</title>
    <style>
        table {
            border-collapse: collapse;
            width: 100%;
        }

        th, td {
            border: 1px solid #ddd;
            text-align: left;
            padding: 8px;
        }

        th {
            background-color: #f2f2f2;
        }

        tr:nth-child(even) {
            background-color: #f2f2f2;
        }
    </style>
</head>
<body>
    <table>
        <thead>
            <tr>
                <th>ID</th>
                <th>Deployed At</th>
            </tr>
        </thead>
        <tbody>
            {% for deployment in deployments %}
                <tr>
                    <td>{{ deployment.id }}</td>
                    <td>{{ deployment.deployed_at | date: "%Y-%m-%d %H:%M:%S" }}</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
</body>
</html>